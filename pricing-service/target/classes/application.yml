server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: pricing-service

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.tp27.pricing: INFO
